[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]

[tool.coverage.run]
omit = [
    "processing/geocode_towns.py",
    "processing/tests/*",
]
