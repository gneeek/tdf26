# Strand: pipeline spine — pre-publish verification + parser hygiene (#322 / #479 / #508 / #326)

The v1.4.20 "spine": the structural pipeline-robustness work that sits behind the gate fixes. Runs **after** the publish-safety gate (#617/#618/#619) merges — the gate owns `scripts/publish.sh` and `processing/weather.py` for its window, and three of these four issues touch that surface. Lands across the **seg 17–19 publish cycles** (target close ~seg 20, 2026-06-10). Not a publication-day deadline; this is the robustness layer the gate fixes are a down-payment on.

## 1. Goal

Land the four spine issues that turn the gate's point-fixes into durable robustness: a real pre-publish verification path (#322), the documented frontmatter convention that stops the YAML-crash class (#479), a shell-test harness that gives the gate fixes regression coverage (#508), and the frontmatter-parser consolidation that de-risks the whole parse path (#326). These are ordered by dependency below; one strand session works them in sequence, opening a PR per issue (or per coherent pair) rather than one mega-PR. Milestone: **v1.4.20 — Publish-pipeline hardening**. Serves the milestone success criterion "a test harness exists that can exercise publish.sh's failure modes (#508), so the three gate fixes have regression coverage."

## 2. Filesystem posture

```
git -C /home/jhs/code/tdf26 worktree add -b feature/pipeline-spine /home/jhs/code/tdf26-spine main
```

- Branch off `main` **after the gate strand has merged** (verify `git log origin/main` shows #617/#618/#619 landed before starting #508 or #326 — see §7). Explicit-path worktree; no nesting, no `.claude` symlink.
- Before each `git add` / `git commit`, run `git branch --show-current` and confirm `feature/pipeline-spine`.
- Fresh worktree has no `node_modules` and no rider-seed data: `npm ci`, then `cp data/riders/*.example.json` to their non-example names before any `npm run build` (per `feedback_ci_seed_ordering.md`).
- #326 touches TypeScript (`server/api/*.ts`) as well as Python — confirm the worktree's `npm ci` covers the server build before editing there.

## 3. Source-of-truth posture

- **Read the scripts and parsers directly; the planning notes describe intent, not the current shape.** Specifically, verify the claim that "publish.sh is one of the four hand-rolled parsers" against source before scoping #326. Issue #326's own four-file list is `processing/validate_entries.py`, `processing/weather.py`, `server/api/entries.get.ts`, `server/api/images.post.ts` — **`scripts/publish.sh` is not in that list.** The scope file (`docs/planning/v1.4.20-scope.md`) and the issue disagree; resolve by reading what each file actually does before committing to the consolidation set. publish.sh reads frontmatter via inline bash, which may or may not be in scope for #326. Flag the discrepancy in the PR body whichever way you resolve it (per `feedback_source_of_truth_framing.md`).
- The four-parser maintainability finding is written up at `metablog/2026-04-10-harness-engineering-dialogue.md` — read it for #326's design intent (shared schema definition).
- For #479: the convention decision (inline JSON, YAML-list support gated on #326) was made at 2026-05-06 planning; the crash history is the seg-9 publish-day incident. Confirm the *current* behaviour of `processing/validate_entries.py` against both frontmatter forms before writing the CLAUDE.md note — do not document a rule the validator no longer enforces.
- For #508: `bats` is the suggested harness (issue names it); `shunit2` and a Python runner are the alternatives. Pick at brief time. Confirm whichever is installable in the worktree/CI before committing the harness.

## 4. Target issues

Milestone **v1.4.20**. Order by dependency, not number:

1. **#479** — document the inline-JSON image-frontmatter convention in CLAUDE.md Phase 2d. Smallest, zero shared-code risk (CLAUDE.md only); do it first to clear it and to fix the convention #326 will later relax.
2. **#508** — shell-test harness + backfill regression tests for the three gate fixes (idempotent merge call = #618, draft pre-flight = #617, weather `--entry` = #619) and the #426 fix shape. **Depends on the gate fixes existing on `main`.**
3. **#322** — pre-publish verification. The gate's #617 draft pre-flight is one piece of this umbrella; the remaining work is the *automated* half (CI tests that rendered pages load: frontmatter parses, key components render, no blank pages) and the *process* half (build-and-preview step in the release checklist — coordinate with #521 in the ceremony strand so the checklist isn't written twice).
4. **#326** — consolidate the hand-rolled frontmatter parsers into one canonical Python parser and one canonical TS parser reading a shared schema; remove the hand-rolled implementations; add a lint/CI guard against new hand-rolled parsing. Largest; **may warrant being split out into its own follow-on strand if it proves too big for this window** (see §8) — it is the one issue here that can slip to the next milestone without breaking the gate.

## 5. Workflow per issue

- **#479:** Add a short subsection to CLAUDE.md Phase 2d (Blog Entry Format) naming inline JSON as the image-frontmatter convention, with a one-line rationale and the `#326` cross-link ("validator currently regex-based; YAML-list support gated on #326"). No code. Tell the seg 18/19 drafting strands the rule directly (they must not reintroduce the YAML form).
- **#508:** Add the harness; backfill regression tests for the gate's three fix points (assert the already-merged-PR path is skipped; assert a `draft: true` target aborts; assert weather lands in the named segment and leaves N-1 untouched). Demonstrate red-green against a restored-broken state (no committed breakage). Add/update `scripts/README.md` with the test-first-for-shell rule.
- **#322:** Build the automated render-check (CI step that generates the site and asserts each entry page renders without a blank/500). Add the build-and-preview process step to the release checklist (hand to #521 / coordinate). Do **not** re-implement the draft pre-flight — that shipped in the gate (#617); reference it as the first verification gate, this issue adds the rest.
- **#326:** Define the shared schema; write the canonical Python parser, migrate `validate_entries.py` + `weather.py`; write the canonical TS parser, migrate `entries.get.ts` + `images.post.ts`; remove the four hand-rolled versions; add the anti-regression lint/CI guard. **Re-verify against the gate's weather.py edit (#619)** — rebase onto it; do not revert the gate's `--entry` handling during the migration.

## 6. Verification commands

- `bash -n scripts/publish.sh` after any script edit.
- The new shell harness (e.g. `bats tests/` or the chosen runner) — once #508 lands, run it for every subsequent issue.
- `npm test` (expect the full suite green).
- `python3 processing/validate_entries.py --entries-dir content/entries --non-interactive`
- `python3 processing/validate_points.py`
- `npm run build` then a `nuxt generate` preview (not the SSR runtime, per `feedback_production_preview.md`) — the #322 render-check should exercise this path.
- Red-green: for #508, show each regression test fires red against the pre-gate state and green against the merged fix. For #326, show the canonical parser produces byte-identical field extraction to the hand-rolled versions on every current entry before deleting them.

## 7. Cross-strand sharing notes

**This is the load-bearing section — the spine contends with the gate and ceremony strands on `scripts/publish.sh` and `processing/weather.py`.**

- **Owns (write):** `CLAUDE.md` (#479), `tests/` or `processing/tests/` + `scripts/README.md` (#508), CI config + any new render-check script (#322), `processing/validate_entries.py`, `processing/weather.py`, `server/api/entries.get.ts`, `server/api/images.post.ts`, and the new shared-schema file (#326).
- **Reads:** `scripts/publish.sh` (#322/#508 test it; do not re-implement its draft pre-flight), `content/entries/*.md` frontmatter (no edits), `data/segments.json`.
- **Must NOT touch:** `scripts/publish.sh`'s release/merge logic (gate + ceremony own those edits); any `content/entries/*.md` body or frontmatter (`feedback_content_change_rule.md`); `data/` content; the wiki Roadmap step (ceremony #480 owns it).
- **Sequencing (hard):**
  - **Start only after the gate (#617/#618/#619) has merged to `main`.** #508 backfills regression tests *for those fixes* — it cannot precede them. #326 edits `weather.py`, which #619 also edits — rebase on the gate, never concurrently.
  - **#479 is the one exception:** it touches only CLAUDE.md and can run any time, even while the gate is still open. Land it first to unblock the drafting strands.
  - **Coordinate the release-checklist edit with the ceremony strand (#521/#480).** #322's process half and #521 both want to write the build-and-preview step — agree one owner (recommend: #322 adds the *verification* step, #521 owns the *checklist artifact*) so they don't collide.
- **Forecast failure mode:** the most likely collision is #326's `weather.py` migration silently reverting the gate's `--entry $SEGMENT` handling. Mitigation: rebase #326 on the merged gate, and add the #508 weather regression test *before* the #326 migration so the migration runs against a red-green guard.

## 8. Scope discipline

- File new issues for findings outside the four-issue write-set; do not over-scope.
- **#326 is the explicit slip candidate.** If the shared-schema + four-file migration proves larger than this window holds, ship #479/#508/#322 and split #326 into its own follow-on strand carried to the next milestone (the data-to-display cycle). Surface this to the publisher via AskUserQuestion the moment #326 looks like it will displace the other three, rather than letting it eat the window silently.
- The #322 / #521 checklist-ownership split is a material checkpoint — resolve it with the ceremony strand or the publisher, don't write the step twice.
- Document any publisher override in the PR body.

## 9. Memories that apply

- `feedback_source_of_truth_framing.md` — the "publish.sh is one of the four parsers" claim needs provenance-tracing before #326 scoping.
- `feedback_ci_seed_ordering.md` — fresh-worktree seed step before build.
- `feedback_assertion_bug_class.md` — work each regression test's diagnostic by hand before writing it (#508).
- `feedback_production_preview.md` — `nuxt generate`, not SSR, for the #322 render-check.
- `feedback_no_regex_in_bash.md` — relevant if any shell test embeds Python.
- `feedback_shared_tree_branch_verification.md`, `feedback_strand_worktree_path.md`, `feedback_strand_session_self_cleanup.md`.
- `feedback_content_change_rule.md` — the parser migration must not alter any published entry's frontmatter.

## 10. Stop when

- PRs open closing #479, #508, #322, and #326 (or #326 split out to a follow-on strand with a tracking note); CI green; red-green demonstrated for #508 and #326.
- The shell harness exists and runs in CI, with the three gate fixes under regression coverage (the milestone success criterion).
- **Cleanup (you run these):** `git -C /home/jhs/code/tdf26 worktree remove /home/jhs/code/tdf26-spine` once merged.
- Final report to publisher: PR links, the resolved #326 scope (consolidated this cycle vs split out), the #322/#521 checklist-ownership decision, and how the "publish.sh is one of four parsers" discrepancy was resolved.
- **Retro inputs written to `project_next_planning_notes.md`** under `## Items surfaced during pipeline-spine strand execution (<date>)`: decision-actionable observations (esp. #326 disposition), light-tier pattern observations, numeric stats (files-touched, commits, harness test count, checkpoints fired, wall-clock).
